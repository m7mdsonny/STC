# تعليمات إعادة التشغيل والاختبار - Restart Instructions

## 🔄 الخطوات لإعادة تشغيل التطبيق والاختبارات

### الخطوة 1: إيقاف السيرفر الحالي (إن وجد)

```powershell
# البحث عن عمليات Node.js/Vite
Get-Process | Where-Object {$_.ProcessName -like "*node*"} | Stop-Process -Force

# أو إيقاف عملية على المنفذ 5173
netstat -ano | findstr :5173
# ثم استخدم Task Manager لإيقاف العملية
```

### الخطوة 2: إعادة تشغيل Dev Server مع الإصلاحات

```powershell
cd "D:\STC\STC AI\apps\web-portal"
npm run dev:host
```

**أو إذا كنت تستخدم npm run dev العادي:**
```powershell
npm run dev
```

**التحقق من أن السيرفر يعمل:**
- افتح http://localhost:5173 في المتصفح
- يجب أن تظهر الصفحة الرئيسية بدون أخطاء
- افتح Developer Console (F12) وتحقق من عدم وجود أخطاء

### الخطوة 3: التحقق من الإصلاحات

#### أ) فتح صفحة Login
```
http://localhost:5173/login
```
- يجب أن تظهر صفحة Login بشكل كامل
- يجب أن تعمل جميع الحقول
- لا يجب أن تكون الصفحة فارغة

#### ب) فتح Developer Console
- اضغط F12
- اذهب إلى Console tab
- يجب ألا توجد أخطاء `ERR_CONTENT_LENGTH_MISMATCH`
- يجب ألا توجد أخطاء `ERR_EMPTY_RESPONSE`

#### ج) فتح Network Tab
- اذهب إلى Network tab في Developer Tools
- أعد تحميل الصفحة (F5)
- تحقق من أن جميع الملفات تتحمل بنجاح (Status 200)
- لا يجب أن تكون هناك ملفات فاشلة

### الخطوة 4: إعادة تشغيل الاختبارات

بعد التأكد من أن التطبيق يعمل بشكل صحيح:

```powershell
cd "D:\STC\STC AI"
node "C:\Users\STC Solutions\AppData\Local\npm-cache\_npx\8ddf6bea01b2519d\node_modules\@testsprite\testsprite-mcp\dist\index.js" generateCodeAndExecute
```

**أو استخدام TestSprite MCP tool:**
```javascript
mcp_TestSprite_testsprite_generate_code_and_execute({
  projectPath: "D:\\STC\\STC AI",
  projectName: "STC AI-VAP",
  testIds: [],
  additionalInstruction: "Re-run tests after Vite fixes"
})
```

---

## 🔍 التحقق من الإصلاحات

### Checklist قبل إعادة الاختبار:

- [ ] Dev Server يعمل على المنفذ 5173
- [ ] الصفحة الرئيسية تفتح بدون أخطاء
- [ ] صفحة Login تظهر بشكل كامل (ليست فارغة)
- [ ] لا توجد أخطاء في Console
- [ ] جميع الملفات تتحمل في Network tab
- [ ] Backend API متاح (إن كان مطلوباً)

---

## 🐛 إذا استمرت المشاكل

### خيار 1: استخدام Production Build

```powershell
cd "D:\STC\STC AI\apps\web-portal"
npm run build
npm run preview:host
```

### خيار 2: تعطيل HMR تماماً

عدّل `vite.config.ts`:
```typescript
server: {
  hmr: false,  // تعطيل HMR
  // ... باقي الإعدادات
}
```

### خيار 3: استخدام serve

```powershell
npm install -g serve
cd "D:\STC\STC AI\apps\web-portal"
npm run build
serve -s dist -l 5173
```

---

## 📊 النتائج المتوقعة بعد الإصلاحات

### قبل الإصلاحات:
- ❌ 15/16 اختبار فشل
- ❌ ERR_CONTENT_LENGTH_MISMATCH
- ❌ ERR_EMPTY_RESPONSE
- ❌ صفحة Login فارغة

### بعد الإصلاحات (متوقع):
- ✅ معظم الاختبارات يجب أن تنجح
- ✅ لا توجد أخطاء Content-Length
- ✅ لا توجد استجابات فارغة
- ✅ صفحة Login تعمل بشكل صحيح

---

## 📝 ملاحظات مهمة

1. **Backend:** تأكد من أن Backend API يعمل إذا كانت الاختبارات تحتاج إليه
2. **Port:** إذا كان المنفذ 5173 مستخدم، قم بإيقاف العملية السابقة
3. **Cache:** قد تحتاج لمسح cache المتصفح (Ctrl+Shift+Delete)
4. **Timeouts:** الاختبارات قد تستغرق 5-15 دقيقة

---

**جاهز لإعادة التشغيل!** 🚀
